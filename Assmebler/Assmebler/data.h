#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_LINE_LENGTH 81 /* the maximum length of a given line in a file*/
#define RESERVED_WORDS_COUNT 30 /* the ammount of reserved words in the assembly language*/
#define MAX_LABEL_LENGTH 30 /* the maximum length of a label name*/
#define MAX_FILE_NAME_LENGTH 40 /* the maximum length of a file name*/
